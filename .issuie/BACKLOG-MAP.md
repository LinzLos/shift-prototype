# Backlog map — LinzLos/shift-prototype

Generated by issuie from `phase4-brief.md` (LedgerChart governance + token-debt). 7 drafts → 4 new shift issues, 2 updates to existing trackers, 1 cross-repo issue.

**Invariant:** within any wave, no two issues share a file in their `Touches` set, and every issue's prerequisites are satisfied by an earlier wave. → Each wave is safe to run with one agent per issue, concurrently, each in its own git worktree.

## Draft → target mapping

| Draft | Becomes | Repo | Note |
|-------|---------|------|------|
| 01 ds:candidate LedgerChart | **comment/edit on existing #2** (Promotion candidates) | shift | not a new issue — appends to the Lane B tracker |
| 02 ds:consumes coupling map | **comment/edit on existing #1** (Coupling map) | shift | not a new issue — appends to the Lane A tracker |
| 03 token-debt RosterModal | new **#3** | shift | buildable |
| 04 token-debt QueueMonitor | new **#4** | shift | buildable |
| 05 token-debt Performance+Simulation | new **#5** | shift | buildable |
| 06 token-debt Roster | new **#6** | shift | buildable |
| 07 upstream tokens (progress + amber) | new issue in **tiny-wire** | vendor (cross-repo) | enabler for the gradient/amber lines only |

Existing open: #1 `ds:consumes`, #2 `ds:candidate`. New shift numbers therefore start at #3.

## Waves

### Wave 1 — parallel-safe (all token-debt, disjoint files)
| Issue | Title | Touches | Labels |
|---|---|---|---|
| #3 | Replace hardcoded hex with tokens in RosterModal | src/components/RosterModal.tsx | chore, ds:consumes, area:roster, P3 |
| #4 | Replace hardcoded hex with tokens in QueueMonitor | src/screens/QueueMonitor.tsx | chore, ds:consumes, area:queue-monitor, P3 |
| #5 | Replace hardcoded hex with tokens in Performance + Simulation | src/screens/Performance.tsx, src/screens/Simulation.tsx | chore, ds:consumes, area:performance, P3 |
| #6 | Replace hardcoded hex with tokens in Roster | src/screens/Roster.tsx | chore, ds:consumes, area:roster, P3 |

All four `Touches` sets are pairwise disjoint → run concurrently, one agent per issue.

### Cross-repo (separate working tree — inherently parallel with the above)
| Issue | Title | Touches (shared) | Labels |
|---|---|---|---|
| tiny-wire #8 | Decide tokens for progress gradient + dark-amber | tiny-wire/lib/tokens.css, tiny-wire/CHANGELOG.md | chore, ds:candidate, area:design-system, P3 |

Lives in the **vendor** repo, so it never collides with shift files. Soft-enables the `#bfc7d4→#8195b5` gradient lines (#4, #6) and `#8a5c00` (#3) **only if** "add token" is chosen over "remap to nearest." Default path: remap to nearest existing token now → these issues do not block.

### Tracker updates (no new issue, no file collision)
- Draft 01 → append LedgerChart candidate assessment to **#2**, add row to `tiny-wire/DESIGN-SYSTEM-HUB.md`.
- Draft 02 → append `--chart-*` bindings + retired-drift note to **#1**.

## Dependency graph
```
tiny-wire #8 ┄┄▶ #3   (soft: #8a5c00 → token, only if "add token")
tiny-wire #8 ┄┄▶ #4   (soft: progress gradient → token)
tiny-wire #8 ┄┄▶ #6   (soft: progress gradient → token)
#5 — independent
01 (→#2), 02 (→#1) — independent tracker updates
```
All edges are **soft** (┄┄▶): each is removed by choosing remap-to-nearest. With remap, Wave 1 runs fully parallel with zero prerequisites.

## Hand-offs (cross-owner dependencies) — the pit stops
None. Single owner (Lindsay) across vendor + consumer → no work changes hands.

## Needs scoping (excluded from waves)
None — every issue has a determined `Touches` set. (Drafts 01/02 touch no code; they update tracker-issue bodies.)

## How to run a wave (multi-agent)
1. Pick the lowest unfinished wave (here: Wave 1).
2. Spawn one agent per issue, each in its own worktree (`issue3`, `issue4`, `issue5`, `issue6`).
3. Merge all; disjoint files by construction → nothing conflicts.
4. Tracker updates (01→#2, 02→#1) and tiny-wire #8 can run anytime alongside.
